import React from "react";
import './Header.css';

const Header = () => {
    return(
        <div className="app-header"> 
            <h1>weather app</h1>
        </div>
    );
};

export default Header;